machine:
  environment:
    SBT_GHPAGES_COMMIT_MESSAGE: 'Publishing Scaladoc [ci skip]'
  java:
    version: oraclejdk8
  services:
    - docker

test:
  override:
    - test.sh

deployment:
  master:
    branch: master
    owner: pathikrit
    commands:
      - deploy.sh
